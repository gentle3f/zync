# Zync — Latest Handoff

## Mandatory first read

Read and obey `AI_STATE/OPERATING_RULES.md` before any write, commit, push, PR,
CI, Vercel, deployment or external-infrastructure action.

Authoritative continuation checkpoint:

`AI_STATE/HANDOFF_20260923_CARD_ART_PC_GAMING_DEDICATED_READY.md`

Supporting defect audit:

`AI_STATE/CARD_ART_CHROME_DEFECT_AUDIT_20260923.md`

Branch:

`card-art-pilot-v1-20260921`

Current state:

> **The two-call follow-up failed both cards. technology.robotics has exhausted the tested baseline routes: Standard repeatedly invents code/screens even in a desk-free, screen-free scene; Klein suppresses screens but remains materially photoreal/macro-product-like and failed scene completeness even after a hard style anchor. Robotics is therefore baseline-excluded/policy-quarantined and must not enter any paid batch. The connector refused the direct write to generation_guardrails_v1.json, so its machine guard entry is still pending for a later normal git-edit session; do not interpret that as permission to generate it. PC Gaming's prior retry failed because collection_object_hero/hands_build imposed a build/gadget prior. A genuine dedicated screenless_pc_play / tower_input_play archetype+variant now exists, with an experiment-only single-call Standard FLUX validation prepared at $0.0125. Run `cd tools/card_art && node src/runPcGamingDedicatedRetry.js --dry-run` first; only if clean, run `node src/runPcGamingDedicatedRetry.js`. Do not auto-reroll or broaden. The rounded light card-border/chrome defect is now confirmed recurring across 2 independent cases (different models/hobbies/archetypes), but clean controls rule out a simple model/route/dimension cause; current classification is stochastic/upstream framing defect with a possible but unproven weak trigger from global collectible/card semantics. Do not crop automatically or rewrite global style yet. sports.american_football remains quarantined. No image API calls were made preparing this checkpoint. Keep Vercel, GitHub Actions, Production and Play closed.**
