# Zync — Latest Handoff

## Mandatory first read

Read and obey `AI_STATE/OPERATING_RULES.md` before any write, commit, push, PR,
CI, Vercel, deployment or external-infrastructure action.

Authoritative continuation checkpoint:

`AI_STATE/HANDOFF_20260923_CARD_ART_SCREEN_MODEL_DISCRIMINATOR_READY.md`

Branch:

`card-art-pilot-v1-20260921`

Current state:

> **The 10-card screen remediation failed 3/10, so the next step is deliberately only a 3-call discriminator and is prepared but NOT generated. An experiment-only override layer now allows scene/archetype replacement without mutating normal production recipes. technology.robotics will use the exact same completely screen-free, desk-free physical robot field-test scene on Standard FLUX and Klein, allowing a direct model-route comparison. gaming.game_streaming gets a separate screenless broadcast-performance scene on Standard, also retesting its prior rounded card-border defect. Estimated total generation cost is only $0.0364. The letterbox detector has also been recalibrated to v2-flatness (row variance/range + tone stability rather than darkness), fixing the known false-positive/false-negative mechanism. Run diagnose-letterbox-v2 + audit-screen-model-discriminator-v1 first; only generate the three calls if clean. American Football remains quarantined. Do not start any broader batch automatically. Keep Vercel, GitHub Actions, Production and Play closed.**
