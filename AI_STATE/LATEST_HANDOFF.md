# Zync — Latest Handoff

## Mandatory first read

Read and obey `AI_STATE/OPERATING_RULES.md` before any write, commit, push, PR,
CI, Vercel, deployment or external-infrastructure action.

Authoritative continuation checkpoint:

`AI_STATE/HANDOFF_20260923_CARD_ART_SCREEN_FOLLOWUP_2_READY.md`

Branch:

`card-art-pilot-v1-20260921`

Current state:

> **The 3-call screen discriminator is complete. Robotics Standard is rejected for this experiment because it invented readable code despite a screen-free, desk-free physical scene. Robotics Klein suppressed the screen but failed house style through severe photoreal/macro-product drift. Game Streaming's screenless physical-recognition route passed cleanly. The next step is prepared but NOT generated: exactly 2 calls, estimated $0.0239. Call 1: Robotics on Klein using the same screen-free field-test scene plus hard painterly-cinematic style anchoring and photographic-style negative_prompt exclusions. Call 2: PC Gaming on Standard using an experiment-only collection_object_hero/hands_build physical setup so recognition comes from desktop tower + keyboard + mouse + headphones + player reaction with the monitor physically absent. Normal production routes remain unchanged. Run `cd tools/card_art && node src/runScreenFollowup2.js --dry-run` first; only generate with `node src/runScreenFollowup2.js` if clean. American Football remains quarantined. Do not start any broad batch automatically. Keep Vercel, GitHub Actions, Production and Play closed.**
